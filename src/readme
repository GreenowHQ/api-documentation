openapi: 3.0.0
info:
  title: Sample API
  description: Optional multiline or single-line description in [CommonMark](http://commonmark.org/help/) or HTML.
  version: 0.1.9
servers:
  - url: http://api-dev.greenpush.com/v1
    description: Greenpush platform (development)
  - url: http://api-qa.greenpush.com/v1
    description: Greenpush platform (qa)
  - url: http://api.example.com/v1
    description: Greenpush platform
paths:
  /estimations:
    get:
      summary: Returns a list of estimations.
       responses:
        '200':    # status code
          description: A JSON array of transaction IDs
          content:
            application/json:
              schema: 
                type: array
                items: 
                  type: string
    get:
      summary: Create one estimations
      responses:
        '200':    # status code
          description: A JSON array of estimation required
          content:
            application/json:
              schema: 
                type: array
                items: 
                  type: string